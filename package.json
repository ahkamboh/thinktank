{
  "name": "thinktank",
  "version": "0.0.0",
  "private": true,
  "description": "thinktank - a local-first universal memory layer that lets every AI coding agent (Cursor, Claude Code, Codex) and web chat (ChatGPT, Claude.ai) share one private brain.",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "packageManager": "pnpm@11.8.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:core": "pnpm --filter @thinktank/core test"
  },
  "license": "MIT"
}
